import { io } from "socket.io-client";

const SOCKET_URL = "http://localhost:5000"; // Change to your backend URL/port

export function Notifications() {
  // Removed the useNotifications hook and its content
}
